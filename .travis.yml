sudo: required

language: go

go:
  - 1.9

services:
  - docker

before_install:
  - sudo apt-get install jq

script:
  - make unittest
  - ./scripts/ci-smoketest.sh

after_script:
  - make destroy-smoketest
