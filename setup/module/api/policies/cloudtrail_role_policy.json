{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": "logs:CreateLogStream",
            "Resource": "arn:aws:logs:${region}:${account_id}:log-group:l0-${name}:log-stream:l0-${name}-cloudtrail*"
        },
        {
            "Effect": "Allow",
            "Action": "logs:PutLogEvents",
            "Resource": "arn:aws:logs:${region}:${account_id}:log-group:l0-${name}:log-stream:l0-${mame}-cloudtrail*"
        }
    ]
}
