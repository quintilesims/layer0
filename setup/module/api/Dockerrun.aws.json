[
    {
        "name": "api",
        "image": "quintilesims/l0-api:${version}",
        "essential": true,
        "memory": 500,
        "portMappings": [
            {
                "hostPort": 80,
                "containerPort": 9090
            }
        ],
        "logConfiguration": {
                "logDriver": "awslogs",
                "options": {
                    "awslogs-group": "${log_group_name}",
                    "awslogs-region": "${region}",
                    "awslogs-stream-prefix": "l0"
                }
         },
        "environment": [
            { "name": "LAYER0_AWS_ACCESS_KEY_ID", "value": "${access_key}" },
            { "name": "LAYER0_AWS_SECRET_ACCESS_KEY", "value": "${secret_key}" },
            { "name": "LAYER0_AWS_REGION", "value": "${region}" },
            { "name": "LAYER0_AWS_PUBLIC_SUBNETS", "value": "${public_subnets}" },
            { "name": "LAYER0_AWS_PRIVATE_SUBNETS", "value": "${private_subnets}" },
            { "name": "LAYER0_AWS_INSTANCE_PROFILE", "value": "${ecs_instance_profile}" },
            { "name": "LAYER0_AWS_VPC", "value": "${vpc_id}" },
            { "name": "LAYER0_AWS_SSH_KEY", "value": "${ssh_key_pair}" },
            { "name": "LAYER0_AWS_S3_BUCKET", "value": "${s3_bucket}" },
            { "name": "LAYER0_INSTANCE", "value": "${l0_instance}" },
            { "name": "LAYER0_AWS_LINUX_AMI", "value": "${linux_service_ami}" },
            { "name": "LAYER0_AWS_WINDOWS_AMI", "value": "${windows_service_ami}" },
            { "name": "LAYER0_AWS_TAG_TABLE", "value": "${dynamo_tag_table}" },
            { "name": "LAYER0_AWS_JOB_TABLE", "value": "${dynamo_job_table}" },
            { "name": "LAYER0_AWS_LOCK_TABLE", "value": "${dynamo_lock_table}" },
	    { "name": "LAYER0_TOKEN", "value": "${api_auth_token}" },
            { "name": "LAYER0_AWS_ACCOUNT_ID", "value": "${account_id}" },
            { "name": "LAYER0_AWS_LOG_GROUP", "value": "${log_group_name}" },
            { "name": "LAYER0_AWS_REQUEST_DELAY", "value": "${request_delay}" },
            { "name": "LAYER0_DEBUG", "value": "true" }
        ]
    }
]
